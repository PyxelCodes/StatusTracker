

const AuthLayout = ({ children }: { children: React.ReactNode }) => {
	return (
	  <div className="h-full w-full flex justify-center items-center pt-80">{children}</div>
	);
  };
  
  export default AuthLayout;
  